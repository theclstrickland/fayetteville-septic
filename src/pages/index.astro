---
import Base from '../layouts/Base.astro';
import { site } from '../data/site';
---

<Base>
  <!-- ============ HERO ============ -->
  <section class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-badge fade-up">Serving {site.county} County & Beyond</div>
        <h1 class="fade-up fade-up-d1">
          {site.city} <span>{site.tagline.split(' & ')[0]}</span><br />& Repair
        </h1>
        <p class="hero-sub fade-up fade-up-d2">
          We provide septic tank pumping, cleaning, repair, and inspection across {site.city}, {
            site.state
          } and surrounding counties. Call now for fast local service and a free estimate.
        </p>
        <div class="hero-actions fade-up fade-up-d3">
          <a href={`tel:${site.phoneTel}`} class="btn-primary pulse">📞 {site.phone}</a>
          <a href="#contact" class="btn-secondary">Request a Free Estimate</a>
        </div>
      </div>
      <div class="hero-visual fade-up fade-up-d4">
        <div class="hero-card">
          <div class="hero-card-header">
            <div class="hero-card-icon">🚛</div>
            <div>
              <div class="hero-card-title">Residential Septic Pumping</div>
              <div class="hero-card-subtitle">{site.city}, {site.state} Metro</div>
            </div>
          </div>
          <div class="hero-stat-grid">
            <div class="hero-stat">
              <div class="hero-stat-label">Starting at</div>
              <div class="hero-stat-value accent">$350</div>
            </div>
            <div class="hero-stat">
              <div class="hero-stat-label">Response Time</div>
              <div class="hero-stat-value green">Same Day</div>
            </div>
            <div class="hero-stat">
              <div class="hero-stat-label">Availability</div>
              <div class="hero-stat-value">24/7</div>
            </div>
            <div class="hero-stat">
              <div class="hero-stat-label">Estimates</div>
              <div class="hero-stat-value green">Free</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ TRUST BAR ============ -->
  <div class="trust-bar">
    <div class="container">
      <div class="trust-items">
        {
          site.trustBadges.map((b) => (
            <div class="trust-item">
              <div class="trust-icon">{b.icon}</div>
              {b.text}
            </div>
          ))
        }
      </div>
    </div>
  </div>

  <!-- ============ SERVICES ============ -->
  <section class="services" id="services">
    <div class="container">
      <div class="section-label">What We Do</div>
      <h2 class="section-title">Septic Tank Services in {site.city}, {site.state}</h2>
      <p class="section-subtitle">
        {site.name} is your local team for septic tank pumping, cleaning, repair, emergency service,
        and certified inspections. Real local crews. Honest pricing. Fast response across the {
          site.city
        } metro.
      </p>
      <div class="services-grid">
        {
          site.services.map((s) => (
            <div class="service-card">
              <div class="service-icon">{s.icon}</div>
              <h3>{s.title}</h3>
              <p>{s.shortDesc}</p>
              <a href={s.linkHref} class="service-link">
                {s.linkText} →
              </a>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ============ PRICING DETAIL ============ -->
  <section class="detail alt" id="pricing">
    <div class="container">
      <div class="detail-content">
        <div class="section-label">Most Popular Service</div>
        <h2>Septic Tank Pumping in {site.city}</h2>
        <p>
          Routine pumping is the single most important service for keeping your system healthy. Skip
          too many and solids migrate into your drain field — turning a $400 pump into a $10,000
          drain field repair.
        </p>
        <p>
          Our local {site.city} crew dispatches same-day for most addresses, with flat-rate pricing confirmed
          before the truck leaves. {site.stateFull}'s {site.region} clay soils make a properly maintained
          tank matter even more than in sandier regions.
        </p>
        <a href={`tel:${site.phoneTel}`} class="btn-primary">Call for Same-Day Service</a>
      </div>
      <div class="detail-visual">
        <h4 class="price-table-heading">{site.city} Area Pricing</h4>
        {
          site.pricing.map((p) => (
            <div class="detail-price-row">
              <span>{p.label}</span>
              <span>{p.value}</span>
            </div>
          ))
        }
        <p class="price-note">
          Prices confirmed on the phone before dispatch. No surprise charges. If we find something
          unexpected on-site, we stop and call you first.
        </p>
      </div>
    </div>
  </section>

  <!-- ============ EMERGENCY CTA ============ -->
  <section class="emergency">
    <div class="container">
      <h2>Septic Emergency in {site.city}? Call Now.</h2>
      <p>Sewage backup, overflow, or tank failure? We answer 24/7 and dispatch within 90–120 minutes.</p>
      <a href={`tel:${site.phoneTel}`} class="btn-primary">📞 Call {site.phone}</a>
    </div>
  </section>

  <!-- ============ LOCATIONS ============ -->
  <section class="locations" id="locations">
    <div class="container">
      <div class="section-label">Service Area</div>
      <h2 class="section-title">Septic Tank Service Near Me: {site.city} Metro</h2>
      <p class="section-subtitle">
        We provide local septic tank service across the {site.city} metro and surrounding counties.
      </p>
      <div class="locations-grid">
        {
          site.locations.map((l) => (
            <a href={`/${l.slug}/`} class="location-card">
              <span class="location-dot" />
              {l.name}
            </a>
          ))
        }
      </div>
      <p class="counties-served">
        <strong>Counties served:</strong>
        {site.counties}.
      </p>
    </div>
  </section>

  <!-- ============ WHY US ============ -->
  <section class="why-us">
    <div class="container">
      <div class="section-label">Why Choose Us</div>
      <h2 class="section-title">Why Choose {site.name}</h2>
      <div class="why-grid">
        {
          site.reasons.map((r) => (
            <div class="why-item">
              <div class="why-icon">{r.icon}</div>
              <div>
                <h4>{r.title}</h4>
                <p>{r.desc}</p>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ============ FAQ ============ -->
  <section class="faq" id="faq">
    <div class="container">
      <div style="text-align:center;">
        <div class="section-label">Common Questions</div>
        <h2 class="section-title">Septic Service Questions, Answered</h2>
      </div>
      <div class="faq-grid">
        {
          site.faqs.map((f) => (
            <details class="faq-item">
              <summary>{f.q}</summary>
              <p>{f.a}</p>
            </details>
          ))
        }
      </div>
    </div>
  </section>

  <!-- FAQ Schema -->
  <script
    type="application/ld+json"
    set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: site.faqs.map((f) => ({
        '@type': 'Question',
        name: f.q,
        acceptedAnswer: { '@type': 'Answer', text: f.a },
      })),
    })}
  />

  <!-- ============ BOTTOM CTA ============ -->
  <section class="bottom-cta" id="contact">
    <div class="container">
      <h2>Reliable Septic Service in {site.city}</h2>
      <p>Real local team. Honest pricing. Fast response. Free estimates.</p>
      <a href={`tel:${site.phoneTel}`} class="btn-primary" style="font-size:18px;padding:18px 36px;"
        >📞 Call {site.phone}</a
      >
    </div>
  </section>
</Base>

<style>
  /* ── Hero ── */
  .hero {
    padding: 80px 0 100px;
    position: relative;
    overflow: hidden;
  }
  .hero::before {
    content: '';
    position: absolute;
    top: -100px;
    right: -200px;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(196, 90, 45, 0.04) 0%, transparent 70%);
    pointer-events: none;
  }
  .hero .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--green-light);
    color: var(--green);
    padding: 6px 14px;
    border-radius: 100px;
    font-size: 13px;
    font-weight: 600;
    margin-bottom: 20px;
    letter-spacing: 0.02em;
  }
  .hero-badge::before {
    content: '';
    width: 7px;
    height: 7px;
    background: var(--green);
    border-radius: 50%;
  }
  .hero h1 {
    font-size: clamp(36px, 4.5vw, 56px);
    font-weight: 800;
    margin-bottom: 20px;
    letter-spacing: -0.02em;
  }
  .hero h1 span {
    color: var(--accent);
  }
  .hero-sub {
    font-size: 18px;
    color: var(--text-light);
    margin-bottom: 32px;
    max-width: 480px;
    line-height: 1.7;
  }
  .hero-actions {
    display: flex;
    gap: 14px;
    flex-wrap: wrap;
  }

  /* ── Hero Card ── */
  .hero-card {
    background: var(--bg-card);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    padding: 32px;
    border: 1px solid var(--border);
  }
  .hero-card-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--border);
  }
  .hero-card-icon {
    width: 44px;
    height: 44px;
    background: var(--accent-light);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
  }
  .hero-card-title {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 15px;
  }
  .hero-card-subtitle {
    font-size: 13px;
    color: var(--text-light);
  }
  .hero-stat-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
  .hero-stat {
    background: var(--bg);
    border-radius: var(--radius);
    padding: 16px;
  }
  .hero-stat-label {
    font-size: 12px;
    color: var(--text-light);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-bottom: 4px;
  }
  .hero-stat-value {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 700;
    color: var(--heading);
  }
  .hero-stat-value.accent {
    color: var(--accent);
  }
  .hero-stat-value.green {
    color: var(--green);
  }

  /* ── Trust Bar ── */
  .trust-bar {
    background: var(--bg-card);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 24px 0;
  }
  .trust-items {
    display: flex;
    justify-content: center;
    gap: 40px;
    flex-wrap: wrap;
  }
  .trust-item {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    font-weight: 500;
    color: var(--text-light);
  }
  .trust-icon {
    width: 36px;
    height: 36px;
    background: var(--green-light);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    flex-shrink: 0;
  }

  /* ── Services ── */
  .services {
    padding: 80px 0;
  }
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
  }
  .service-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 28px;
    transition: box-shadow 0.3s, transform 0.2s;
    position: relative;
    overflow: hidden;
  }
  .service-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-3px);
  }
  .service-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: var(--accent);
    opacity: 0;
    transition: opacity 0.3s;
  }
  .service-card:hover::after {
    opacity: 1;
  }
  .service-icon {
    font-size: 28px;
    margin-bottom: 16px;
  }
  .service-card h3 {
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 10px;
  }
  .service-card p {
    font-size: 14px;
    color: var(--text-light);
    line-height: 1.7;
    margin-bottom: 16px;
  }
  .service-link {
    font-size: 14px;
    font-weight: 600;
    color: var(--accent);
    display: inline-flex;
    align-items: center;
    gap: 4px;
    transition: gap 0.2s;
  }
  .service-link:hover {
    gap: 8px;
  }

  /* ── Detail / Pricing ── */
  .detail {
    padding: 80px 0;
  }
  .detail.alt {
    background: var(--bg-alt);
  }
  .detail .container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }
  .detail-content h2 {
    font-size: clamp(26px, 2.8vw, 36px);
    font-weight: 800;
    margin-bottom: 16px;
    letter-spacing: -0.02em;
  }
  .detail-content p {
    font-size: 16px;
    color: var(--text-light);
    margin-bottom: 16px;
    line-height: 1.8;
  }
  .detail-visual {
    background: var(--bg-card);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    padding: 24px;
    box-shadow: var(--shadow);
  }
  .price-table-heading {
    font-family: var(--font-body);
    font-size: 14px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--text-light);
    margin-bottom: 16px;
  }
  .detail-price-row {
    display: flex;
    justify-content: space-between;
    padding: 14px 0;
    border-bottom: 1px solid var(--border);
    font-size: 15px;
  }
  .detail-price-row:last-of-type {
    border-bottom: none;
  }
  .detail-price-row span:first-child {
    color: var(--text-light);
  }
  .detail-price-row span:last-child {
    font-weight: 700;
    color: var(--heading);
  }
  .price-note {
    font-size: 13px;
    color: var(--text-light);
    margin-top: 16px;
    line-height: 1.6;
  }

  /* ── Emergency ── */
  .emergency {
    background: var(--bg-dark);
    color: var(--text-inverse);
    padding: 60px 0;
    text-align: center;
  }
  .emergency h2 {
    color: #fff;
    font-size: clamp(26px, 3vw, 38px);
    margin-bottom: 12px;
  }
  .emergency p {
    color: rgba(255, 255, 255, 0.7);
    font-size: 17px;
    margin-bottom: 28px;
  }
  .emergency .btn-primary {
    font-size: 18px;
    padding: 18px 36px;
  }

  /* ── Locations ── */
  .locations {
    padding: 80px 0;
  }
  .locations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 14px;
    margin-top: 32px;
  }
  .location-card {
    display: flex;
    align-items: center;
    gap: 10px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 16px 20px;
    font-weight: 500;
    font-size: 15px;
    transition: border-color 0.2s, box-shadow 0.2s;
  }
  .location-card:hover {
    border-color: var(--accent);
    box-shadow: var(--shadow);
  }
  .location-dot {
    width: 8px;
    height: 8px;
    background: var(--green);
    border-radius: 50%;
    flex-shrink: 0;
  }
  .counties-served {
    font-size: 14px;
    color: var(--text-light);
    margin-top: 20px;
  }

  /* ── Why Us ── */
  .why-us {
    padding: 80px 0;
    background: var(--bg-alt);
  }
  .why-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 20px;
    margin-top: 40px;
  }
  .why-item {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }
  .why-icon {
    width: 40px;
    height: 40px;
    background: var(--accent-light);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    flex-shrink: 0;
  }
  .why-item h4 {
    font-family: var(--font-body);
    font-weight: 700;
    font-size: 15px;
    margin-bottom: 4px;
  }
  .why-item p {
    font-size: 14px;
    color: var(--text-light);
    line-height: 1.6;
  }

  /* ── FAQ ── */
  .faq {
    padding: 80px 0;
  }
  .faq-grid {
    max-width: 780px;
    margin: 40px auto 0;
  }
  .faq-item {
    border-bottom: 1px solid var(--border);
  }
  .faq-item summary {
    padding: 20px 0;
    font-weight: 600;
    font-size: 16px;
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    color: var(--heading);
  }
  .faq-item summary::-webkit-details-marker {
    display: none;
  }
  .faq-item summary::after {
    content: '+';
    font-size: 22px;
    font-weight: 300;
    color: var(--accent);
    transition: transform 0.2s;
  }
  .faq-item[open] summary::after {
    content: '−';
  }
  .faq-item p {
    padding: 0 0 20px;
    font-size: 15px;
    color: var(--text-light);
    line-height: 1.7;
  }

  /* ── Bottom CTA ── */
  .bottom-cta {
    padding: 80px 0;
    background: var(--bg-alt);
    text-align: center;
  }
  .bottom-cta h2 {
    font-size: clamp(28px, 3vw, 40px);
    margin-bottom: 12px;
  }
  .bottom-cta p {
    color: var(--text-light);
    font-size: 17px;
    margin-bottom: 28px;
  }

  /* ── Responsive ── */
  @media (max-width: 900px) {
    .hero .container {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .hero-visual {
      order: -1;
    }
    .detail .container {
      grid-template-columns: 1fr;
      gap: 32px;
    }
  }
  @media (max-width: 680px) {
    .hero {
      padding: 48px 0 60px;
    }
    .hero-stat-grid {
      grid-template-columns: 1fr;
    }
    .services-grid {
      grid-template-columns: 1fr;
    }
    .why-grid {
      grid-template-columns: 1fr;
    }
    .trust-items {
      gap: 20px;
    }
    .locations-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
